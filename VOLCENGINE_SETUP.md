# 🗻 火山引擎豆包API配置指南

## 📋 你的API信息

请确认一下你的火山引擎API信息：

### 1. API Key格式
你提到的格式：`000d90d3-8e36-4480-a...` ✓

### 2. Base URL（接入点）
火山引擎豆包API的Base URL通常是：
- `https://ark.cn-beijing.volces.com/api/v3`
- 或者你有其他的接入点？

### 3. 模型ID
- 格式是 `ep-xxxxxx` 吗？
- 完整的模型ID是什么？

---

## 🔧 方案A：修改代码支持火山引擎API

我可以帮你修改代码，让它支持直接配置火山引擎API。

### 修改内容包括：

1. **更新 `.env.example** - 添加火山引擎配置选项
2. **修改 `src/agents/agent.py`** - 支持自定义Base URL和模型ID
3. **更新工具** - 如果需要的话，也更新工具中的API调用

---

## 🔧 方案B：双配置支持（推荐）

我可以帮你创建一个灵活的配置，同时支持：
- ✅ Coze平台（原来的配置）
- ✅ 火山引擎豆包API（你的配置）

通过环境变量切换使用哪个平台。

---

## 📝 在修改之前，请告诉我

为了给你正确的配置，请提供：

### 必填信息
1. **你的火山引擎API Key**（可以只给格式，不用给完整密钥）
   - 确认是 `000d90d3-8e36-4480-a...` 格式吗？

2. **火山引擎Base URL**
   - 是 `https://ark.cn-beijing.volces.com/api/v3` 吗？
   - 还是其他地址？

3. **你的模型ID**
   - 完整的模型ID是什么？
   - 是 `ep-xxxxxx` 格式吗？

### 可选信息
4. **你想用哪个模型？**
   - Doubao 2.0 Pro？
   - 还是其他模型？

---

## 🎯 快速确认

如果你确认：
- Base URL: `https://ark.cn-beijing.volces.com/api/v3`
- 模型ID: `ep-你的模型ID`
- API Key: `000d90d3-8e36-4480-a...` 格式

我现在就可以帮你修改代码！请确认这些信息是否正确。
